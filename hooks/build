#!/bin/bash

docker build -t $IMAGE_NAME --build-arg PAYLOAD_URL=$PAYLOAD_URL --build-arg FRONTEND_URL=$FRONTEND_URL --build-arg TURNSTILE_SITE_KEY=$TURNSTILE_SITE_KEY .
docker tag $IMAGE_NAME $DOCKER_REPO:$SOURCE_COMMIT
docker push $DOCKER_REPO:$SOURCE_COMMIT
